# Athlos mobile - environment variables (placeholder)

# Backend base URL - required to run the app (src/shared/env.ts raises a
# clear error if unset). Expo inlines EXPO_PUBLIC_* vars at build time,
# no extra library needed.
# EXPO_PUBLIC_API_URL=http://localhost:8000
