# Sync

Sincronizacion offline-first entre dispositivos: cola de cambios locales,
resolucion de conflictos y estado de sincronizacion. Contraparte en el
cliente movil de `frontend/mobile/src/data/sync`.
