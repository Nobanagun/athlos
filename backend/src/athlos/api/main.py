"""Application entrypoint (placeholder).

The FastAPI app factory, router registration and startup/shutdown wiring
will be implemented once dependencies are declared in pyproject.toml and
installed. Intentionally left without imports until then.
"""
