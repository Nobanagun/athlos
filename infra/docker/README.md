# Docker

Los Dockerfiles del backend y de los servicios de apoyo (base de datos,
cache, broker) se anadiran cuando se defina el stack de dependencias.
`docker-compose.yml` es por ahora un placeholder sin servicios activos.
